// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a;
    
    for(a = 0; a <= 1; a++) {
        printf("A: %d  ! %d = %d\n", a, a, !a);
    }
    
    return 0;
}